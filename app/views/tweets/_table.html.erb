<div class="box">
  <table class="highlight table-color">
    <thead>
      <tr>
          <th>State</th>
          <th>Bernie Tweets</th>
          <th>Trump Tweets</th>
          <th>Winner</th>
      </tr>
    </thead>

    <tbody>
      <% cache("tweets-breakdown5") do %>
        <% Tweet.state_codes.each do |state| %>
          <tr>
            <td>
              <%= link_to "#{State.state_hash[state]}",
                  state_path(State.find_by(name: (State.state_hash[state])).id) %>
            </td>
            <td>
              <%= Tweet.bernie_by_state(state) %>
            </td>
            <td>
              <%= Tweet.trump_by_state(state) %>
            </td>
            <td>
              <%= Tweet.winning_margin(state) %>
            </td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
